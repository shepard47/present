present:
	canvas:
		editor
		binary file loader
	farbfeld fread bad size (test)
	UTF-8:
		win32
		xlib
	transformations:
		tiles/animation
		rotate (+SIMD)
		flip/mirror
	free memory!
	implementations:
		d3d (WIP)
		wayland
		xcb + vulkan



